name: Hello workflow

on:
 push:
  branches: 
   - 'main'
   - 'develop'
  paths-ignore:
   - 'README.md'
 workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "HELLO FROM MY FIRST WORKFLOW"
