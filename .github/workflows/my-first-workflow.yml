name: Hello workflow

on:
 push:
  branches: 
   - 'main'
   - 'develop'
 workflow_dispatch:

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "HELLO FROM MY FIRST WORKFLOW"
